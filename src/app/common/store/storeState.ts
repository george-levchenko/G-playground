import {ActionReducerMap} from '@ngrx/store';

export const gStoreReducers: ActionReducerMap<any> = {
  // common: gCommonReducer,
  // auth: gAuthReducer,
  // movies: gMoviesReducer
};

export const gStoreEffects: any[] = [

];
